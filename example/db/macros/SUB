<!--- @SUB((subsection_name), (anchor_name), (indent_level), (no_index)) --->

<div class="submain">
$(if $1,
	<div class="page-anchor">
		<h2>$(anchor $(if $2,$2,$(sanitize $1)), $1, $3, $4)</h2>
	</div>
)
